### Tool for getting diff from json and yaml files in different formats

#### Mixing file formats is available (e.g., yaml and json comparison)

```sh
$ gendiff file1 file2 --format [json | stylish | plain]
```
